interface:
  display_name: "Market Signal Analysis"
  short_description: "Market regimes, behavior modes, flows, and return priors."
  default_prompt: "Use $market-signal-analysis to classify the market state, choose the highest-return-per-unit-risk eligible behavior, estimate the net return range from calibrated priors after costs and exit or ruin paths, and state invalidation triggers."
